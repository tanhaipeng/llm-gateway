# LLM Gateway 配置文件
# 复制此文件为 config.yaml 并根据需要修改

server:
  address: "0.0.0.0"
  port: 8080

# Providers 配置
# 注意：API keys 应该从环境变量加载，不要在此文件中直接配置
providers:
  # mistral:
  #   models:
  #     - "ministral-8b"
  #     - "mistral-small"
  #     - "mistral-medium"
  #     - "mistral-large"
  #     - "codestral"
  #   base-url: "https://api.mistral.ai"

  # groq:
  #   models:
  #     - "gemma2-9b-it"
  #     - "llama-3.1-8b-instant"
  #     - "llama-3.3-70b-versatile"
  #   base-url: "https://api.groq.com/openai"

  # deepseek:
  #   models:
  #     - "deepseek-chat"
  #     - "deepseek-reasoner"
  #   base-url: "https://api.deepseek.com"

  # timesniper provider
  timesniper:
    models:
      - "gpt-5.3-codex"
    base-url: "https://api.xcode.best"
    protocol: "responses"  # 如果该 provider 仅支持 /v1/responses，取消注释
